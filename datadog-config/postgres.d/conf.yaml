#rem pza
init_config:

instances:
  - dbm: true
    host: db
    port: 5432
    username: datadog
    password: datadogpassword
    dbname: ddtimerdb
    tags:
      - env:dev-local
      - service:postgres